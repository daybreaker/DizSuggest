<div class="comment">
  <%= comment.body %>
  <% if comment.has_children? %>
    <div class="comment_children">
      <% comment.children.each do |x| %>
        <%= render x %>
      <% end %>
    </div>  
  <% end %>
</div>
